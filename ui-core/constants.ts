export const FAILED_REQUIREMENT = {
    RuntimeEnvironmentCheckRequirement: 'RuntimeEnvironmentCheckRequirement',
};

export const UNAUTHORIZED_INITIALIZED_ERROR_MESSAGES = {
    RuntimeEnvironmentCheckRequirement: 'Running a Flow in this environment requires an active build user session',
    Default: 'User is unauthorized to run the Flow',
};
